import 'package:flutter/material.dart';

class GetStartesScreen extends StatelessWidget {
  const GetStartesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}